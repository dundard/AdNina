@{
    ViewData["Title"] = "Rakip Analizi";
}

<!-- Setup Competitors Page (003-2) -->
<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <div class="grid grid-cols-1 lg:grid-cols-12 gap-8">
        
        <!-- Left Column - Detailed Analysis -->
        <div class="lg:col-span-7">
            
            <!-- Tab Navigation -->
            <div class="bg-white rounded-xl2 shadow-card overflow-hidden mb-6">
                <div class="border-b border-gray-200">
                    <nav class="flex space-x-8 px-6">
                        <button id="summary-tab" class="tab-button py-4 px-2 border-b-2 font-medium text-sm whitespace-nowrap">
                            İşletmenizin Özeti
                        </button>
                        <button id="competitors-tab" class="tab-button active py-4 px-2 border-b-2 font-medium text-sm whitespace-nowrap">
                            Rakipler
                        </button>
                    </nav>
                </div>
                
                <!-- Tab Content -->
                <div class="p-6">
                    <!-- Competitors Analysis Content -->
                    <div id="competitors-content" class="space-y-8">
                        
                        <!-- Hot Topics Section -->
                        <div class="analysis-section">
                            <h3 class="heading text-lg font-semibold text-text mb-4 flex items-center">
                                <div class="w-8 h-8 bg-red-100 rounded-lg flex items-center justify-center mr-3">
                                    <svg class="w-5 h-5 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13.828 10.172a4 4 0 00-5.656 0l-4 4a4 4 0 105.656 5.656l1.102-1.101m-.758-4.899a4 4 0 005.656 0l4-4a4 4 0 00-5.656-5.656l-1.1 1.1"></path>
                                    </svg>
                                </div>
                                Hot Topics / Güncel Trendler
                            </h3>
                            <div class="bg-gray-50 rounded-lg p-4">
                                <p class="text-muted leading-relaxed" id="hot-topics-content">
                                    <!-- Content will be populated by JS -->
                                </p>
                            </div>
                        </div>
                        
                        <!-- Regional Market Section -->
                        <div class="analysis-section">
                            <h3 class="heading text-lg font-semibold text-text mb-4 flex items-center">
                                <div class="w-8 h-8 bg-blue-100 rounded-lg flex items-center justify-center mr-3">
                                    <svg class="w-5 h-5 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
                                    </svg>
                                </div>
                                Regional Market / Bölgesel Pazar
                            </h3>
                            <div class="bg-gray-50 rounded-lg p-4">
                                <p class="text-muted leading-relaxed" id="regional-content">
                                    <!-- Content will be populated by JS -->
                                </p>
                            </div>
                        </div>
                        
                        <!-- Community Reviews Section -->
                        <div class="analysis-section">
                            <h3 class="heading text-lg font-semibold text-text mb-4 flex items-center">
                                <div class="w-8 h-8 bg-green-100 rounded-lg flex items-center justify-center mr-3">
                                    <svg class="w-5 h-5 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"></path>
                                    </svg>
                                </div>
                                Community Reviews / Topluluk Yorumları
                            </h3>
                            <div class="bg-gray-50 rounded-lg p-4">
                                <p class="text-muted leading-relaxed" id="reviews-content">
                                    <!-- Content will be populated by JS -->
                                </p>
                            </div>
                        </div>
                        
                        <!-- Consolidated Plan Section -->
                        <div class="analysis-section">
                            <h3 class="heading text-lg font-semibold text-text mb-4 flex items-center">
                                <div class="w-8 h-8 bg-purple-100 rounded-lg flex items-center justify-center mr-3">
                                    <svg class="w-5 h-5 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                                    </svg>
                                </div>
                                Consolidated Plan / Konsolide Plan
                            </h3>
                            <div class="bg-gradient-to-r from-primary/5 to-primary/10 rounded-lg p-4 border border-primary/20">
                                <p class="text-text leading-relaxed mb-4" id="consolidated-text">
                                    <!-- Content will be populated by JS -->
                                </p>
                                
                                <!-- Key Insights -->
                                <div class="space-y-2">
                                    <h4 class="font-semibold text-text mb-3">🔑 Anahtar İçgörüler:</h4>
                                    <ul id="key-insights" class="space-y-2">
                                        <!-- Bullet points will be populated by JS -->
                                    </ul>
                                </div>
                            </div>
                        </div>
                        
                    </div>
                </div>
            </div>
            
        </div>
        
        <!-- Right Column - Form Card (Same as Summary) -->
        <div class="lg:col-span-5">
            <div class="bg-white rounded-xl2 shadow-card p-6 sticky top-24">
                <h3 class="heading text-lg font-semibold text-text mb-6">
                    Kampanya Detayları
                </h3>
                
                <form id="setup-form" class="space-y-6">
                    
                    <!-- Project Name -->
                    <div>
                        <label for="project-name" class="block text-sm font-medium text-text mb-2">
                            Proje Adı
                        </label>
                        <input 
                            type="text" 
                            id="project-name"
                            name="projectName"
                            placeholder="Kampanya adını girin"
                            class="w-full px-4 py-3 border border-gray-300 rounded-input focus:outline-none focus:ring-2 focus:ring-primary/20 focus:border-primary transition-all"
                        />
                    </div>
                    
                    <!-- Goal Selection -->
                    <div>
                        <label class="block text-sm font-medium text-text mb-3">
                            Hedef
                        </label>
                        <div class="grid grid-cols-1 gap-2">
                            <div class="goal-option" data-goal="Traffic">
                                <input type="radio" id="goal-traffic" name="goal" value="Traffic" class="sr-only">
                                <label for="goal-traffic" class="goal-label">
                                    <div class="flex items-center space-x-3">
                                        <span class="text-2xl">🌐</span>
                                        <div>
                                            <div class="font-medium text-text">Web Sitesi Trafiği</div>
                                            <div class="text-sm text-muted">Sitenize daha fazla ziyaretçi çekin</div>
                                        </div>
                                    </div>
                                </label>
                            </div>
                            
                            <div class="goal-option" data-goal="Leads">
                                <input type="radio" id="goal-leads" name="goal" value="Leads" class="sr-only">
                                <label for="goal-leads" class="goal-label">
                                    <div class="flex items-center space-x-3">
                                        <span class="text-2xl">📧</span>
                                        <div>
                                            <div class="font-medium text-text">Potansiyel Müşteri</div>
                                            <div class="text-sm text-muted">Form doldurmalarını artırın</div>
                                        </div>
                                    </div>
                                </label>
                            </div>
                            
                            <div class="goal-option" data-goal="Sales">
                                <input type="radio" id="goal-sales" name="goal" value="Sales" class="sr-only">
                                <label for="goal-sales" class="goal-label">
                                    <div class="flex items-center space-x-3">
                                        <span class="text-2xl">💰</span>
                                        <div>
                                            <div class="font-medium text-text">Satış</div>
                                            <div class="text-sm text-muted">Doğrudan satış artışı hedefleyin</div>
                                        </div>
                                    </div>
                                </label>
                            </div>
                        </div>
                    </div>
                    
                    <!-- CTA Selection -->
                    <div>
                        <label for="cta-select" class="block text-sm font-medium text-text mb-2">
                            Dönüşüm Düğmesi
                        </label>
                        <select 
                            id="cta-select"
                            name="cta"
                            class="w-full px-4 py-3 border border-gray-300 rounded-input focus:outline-none focus:ring-2 focus:ring-primary/20 focus:border-primary transition-all"
                        >
                            <option value="">Seçiniz</option>
                            
                            <!-- Options will be populated by JS -->
                        </select>
                    </div>
                    
                    <!-- Date Range -->
                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label for="start-date" class="block text-sm font-medium text-text mb-2">
                                Başlangıç
                            </label>
                            <input 
                                type="text" 
                                id="start-date"
                                name="startAt"
                                placeholder="Tarih seçin"
                                class="w-full px-4 py-3 border border-gray-300 rounded-input focus:outline-none focus:ring-2 focus:ring-primary/20 focus:border-primary transition-all"
                            />
                        </div>
                        <div>
                            <label for="end-date" class="block text-sm font-medium text-text mb-2">
                                Bitiş
                            </label>
                            <input 
                                type="text" 
                                id="end-date"
                                name="endAt"
                                placeholder="Tarih seçin"
                                class="w-full px-4 py-3 border border-gray-300 rounded-input focus:outline-none focus:ring-2 focus:ring-primary/20 focus:border-primary transition-all"
                            />
                        </div>
                    </div>
                    
                    <!-- Locations -->
                    <div>
                        <label class="block text-sm font-medium text-text mb-2">
                            Şehir & Ülkeler
                        </label>
                        <div id="location-tags"></div>
                    </div>
                    
                    <!-- Google Maps -->
                    <div>
                        <label class="block text-sm font-medium text-text mb-2">
                            Konum
                        </label>
                        <div class="w-full h-48 bg-gray-100 rounded-lg overflow-hidden">
                            <iframe 
                                src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3010.1472964929987!2d28.97658831539495!3d41.00665792929578!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x14cab9bd6570f6e1%3A0xe1dcbdf02b7e2cb6!2sIstanbul%2C%20Turkey!5e0!3m2!1sen!2s!4v1635789012345!5m2!1sen!2s"
                                width="100%" 
                                height="100%" 
                                style="border:0;" 
                                allowfullscreen="" 
                                loading="lazy">
                            </iframe>
                        </div>
                    </div>
                    
                </form>
            </div>
        </div>
        
    </div>
</div>

@section Scripts {
<script type="text/babel">
    // Mount TagInput component
    const tagInputRoot = ReactDOM.createRoot(document.getElementById('location-tags'));
    
    function LocationTagInput() {
        const [locations, setLocations] = React.useState([]);
        
        React.useEffect(() => {
            const state = loadState();
            if (state.locations) {
                setLocations(state.locations);
            }
        }, []);
        
        const handleLocationsChange = (newLocations) => {
            setLocations(newLocations);
            saveState({ locations: newLocations });
        };
        
        // Generate suggestions from enhanced location data
        const generateSuggestions = () => {
            let suggestions = [];
            
            if (window.AppData && window.AppData.locationData) {
                const { countries } = window.AppData.locationData;
                
                // Add countries
                countries.forEach(country => {
                    suggestions.push(country.name);
                    
                    // Add cities for each country
                    country.cities.forEach(city => {
                        suggestions.push(`${city}, ${country.name}`);
                        suggestions.push(city); // Also add city without country for easy search
                    });
                });
            }
            
            // Fallback to simple locations if enhanced data not available
            if (suggestions.length === 0) {
                suggestions = window.AppData ? window.AppData.locations : [
                    'Turkey', 'Istanbul', 'Ankara', 'Izmir', 'Bursa', 'Antalya'
                ];
            }
            
            // Remove duplicates and sort
            return [...new Set(suggestions)].sort();
        };
        
        const suggestions = generateSuggestions();
        
        return (
            <TagInput 
                value={locations}
                onChange={handleLocationsChange}
                suggestions={suggestions}
                placeholder="Şehir veya ülke ekleyin..."
                maxTags={10}
                showCount={true}
            />
        );
    }
    
    tagInputRoot.render(<LocationTagInput />);
</script>

<script>
    document.addEventListener('DOMContentLoaded', function() {
        const state = loadState();
        
        // Initialize tabs
        initTabs();
        
        // Initialize flatpickr for dates
        initDatePickers();
        
        // Populate form with saved state
        populateForm();
        
        // Initialize CTA options
        initCTAOptions();
        
        // Load competitors analysis
        loadCompetitorsAnalysis();
        
        // Bind form changes to state
        bindFormToState('setup-form', ['projectName', 'goal', 'cta', 'startAt', 'endAt']);
        
        function initTabs() {
            const summaryTab = document.getElementById('summary-tab');
            const competitorsTab = document.getElementById('competitors-tab');
            
            summaryTab.addEventListener('click', function() {
                window.location.href = '/setup/summary';
            });
            
            // Competitors tab is already active (no click handler needed)
        }
        
        function initDatePickers() {
            const startDateEl = document.getElementById('start-date');
            const endDateEl = document.getElementById('end-date');
            
            const startPicker = flatpickr(startDateEl, {
                locale: 'tr',
                dateFormat: 'd.m.Y',
                minDate: 'today',
                onChange: function(selectedDates) {
                    if (selectedDates[0]) {
                        endPicker.set('minDate', selectedDates[0]);
                        const dateStr = selectedDates[0].toISOString();
                        saveState({ startAt: dateStr });
                    }
                }
            });
            
            const endPicker = flatpickr(endDateEl, {
                locale: 'tr',
                dateFormat: 'd.m.Y',
                minDate: 'today',
                onChange: function(selectedDates) {
                    if (selectedDates[0]) {
                        const dateStr = selectedDates[0].toISOString();
                        saveState({ endAt: dateStr });
                    }
                }
            });
            
            // Load saved dates
            if (state.startAt) {
                startPicker.setDate(new Date(state.startAt));
            }
            if (state.endAt) {
                endPicker.setDate(new Date(state.endAt));
            }
        }
        
        function populateForm() {
            // Project name
            const projectNameEl = document.getElementById('project-name');
            if (projectNameEl && state.projectName) {
                projectNameEl.value = state.projectName;
            }
            
            // Goal selection
            if (state.goal) {
                const goalRadio = document.querySelector(`input[name="goal"][value="${state.goal}"]`);
                if (goalRadio) {
                    goalRadio.checked = true;
                    updateGoalSelection(state.goal);
                }
            }
            
            // CTA selection
            if (state.cta) {
                const ctaSelect = document.getElementById('cta-select');
                if (ctaSelect) {
                    ctaSelect.value = state.cta;
                }
            }
        }
        
        function initCTAOptions() {
            const ctaSelect = document.getElementById('cta-select');
            const allOptions = window.AppData ? window.AppData.allCtaOptions : [
                'Daha Fazla Bilgi',
                'Ücretsiz Konsültasyon',
                'Randevu Al',
                'Hemen Ara'
            ];
            
            // Clear existing options except the first one
            ctaSelect.innerHTML = '<option value="">Seçiniz</option>';
            
            // Group options by category if available
            if (window.AppData && window.AppData.ctaOptions) {
                const categories = {
                    'Konsültasyon': window.AppData.ctaOptions.consultation,
                    'Randevu': window.AppData.ctaOptions.appointment,
                    'İletişim': window.AppData.ctaOptions.contact,
                    'Bilgi': window.AppData.ctaOptions.information,
                    'Video': window.AppData.ctaOptions.video,
                    'Test': window.AppData.ctaOptions.test,
                    'Özel Teklifler': window.AppData.ctaOptions.offer
                };
                
                Object.keys(categories).forEach(categoryName => {
                    const optgroup = document.createElement('optgroup');
                    optgroup.label = categoryName;
                    
                    categories[categoryName].forEach(option => {
                        const optionEl = document.createElement('option');
                        optionEl.value = option;
                        optionEl.textContent = option;
                        optgroup.appendChild(optionEl);
                    });
                    
                    ctaSelect.appendChild(optgroup);
                });
            } else {
                // Fallback: simple options
                allOptions.forEach(option => {
                    const optionEl = document.createElement('option');
                    optionEl.value = option;
                    optionEl.textContent = option;
                    ctaSelect.appendChild(optionEl);
                });
            }
            
            // Set saved value
            if (state.cta) {
                ctaSelect.value = state.cta;
            }
        }
        
        function loadCompetitorsAnalysis() {
            // Show loading state first
            const sections = document.querySelectorAll('.analysis-section');
            sections.forEach(section => {
                section.style.opacity = '0.5';
            });
            
            if (window.AppData && window.AppData.analysis && window.AppData.analysis.competitors) {
                const competitors = window.AppData.analysis.competitors;
                
                // Hot Topics
                const hotTopicsEl = document.getElementById('hot-topics-content');
                if (hotTopicsEl) {
                    hotTopicsEl.textContent = competitors.hotTopics;
                }
                
                // Regional Market
                const regionalEl = document.getElementById('regional-content');
                if (regionalEl) {
                    regionalEl.textContent = competitors.regional;
                }
                
                // Community Reviews
                const reviewsEl = document.getElementById('reviews-content');
                if (reviewsEl) {
                    reviewsEl.textContent = competitors.reviews;
                }
                
                // Consolidated Plan
                const consolidatedEl = document.getElementById('consolidated-text');
                if (consolidatedEl) {
                    consolidatedEl.textContent = competitors.consolidated.text;
                }
                
                // Key Insights
                const insightsList = document.getElementById('key-insights');
                if (insightsList && competitors.consolidated.bullets) {
                    insightsList.innerHTML = ''; // Clear existing content
                    competitors.consolidated.bullets.forEach(bullet => {
                        const li = document.createElement('li');
                        li.className = 'flex items-start space-x-2 mb-2';
                        li.innerHTML = `
                            <span class="w-2 h-2 bg-primary rounded-full mt-2 flex-shrink-0"></span>
                            <span class="text-text">${bullet}</span>
                        `;
                        insightsList.appendChild(li);
                    });
                }
                
                // Animate sections in with delay
                setTimeout(() => {
                    sections.forEach((section, index) => {
                        setTimeout(() => {
                            section.style.opacity = '1';
                            section.classList.add('fade-in');
                        }, index * 150);
                    });
                }, 100);
                
                // Show success message
                setTimeout(() => {
                    if (window.showToast) {
                        showToast('Rakip analizi yüklendi!', 'success', 2000);
                    }
                }, sections.length * 150 + 500);
                
            } else {
                // Show fallback content if no data available
                const fallbackContent = {
                    'hot-topics-content': 'Göz sağlığı sektöründe güncel trendler arasında minimal invaziv teknikler, kişiselleştirilmiş tedavi yaklaşımları ve teknoloji destekli ameliyat yöntemleri öne çıkıyor.',
                    'regional-content': 'İstanbul bölgesinde göz sağlığı hizmetleri konusunda yoğun rekabet bulunuyor. Başlıca rakipler arasında özel göz hastaneleri ve klinikler yer alıyor.',
                    'reviews-content': 'Hasta yorumları analizi gösteriyor ki başarılı ameliyat sonuçları, doktor-hasta iletişimi ve post-operatif takip kalitesi en çok değer verilen faktörler.',
                    'consolidated-text': 'Stratejik analiz sonuçlarımıza göre, premium göz sağlığı hizmetleri pazarında güçlü bir konumunuz bulunuyor.'
                };
                
                Object.keys(fallbackContent).forEach(id => {
                    const element = document.getElementById(id);
                    if (element) {
                        element.textContent = fallbackContent[id];
                    }
                });
                
                // Add fallback key insights
                const insightsList = document.getElementById('key-insights');
                if (insightsList) {
                    const fallbackInsights = [
                        'Uzman doktor kadronuzu ve teknolojik altyapınızı öne çıkaran güven odaklı mesajlaşma',
                        'Hasta deneyimlerini merkeze alan sosyal kanıt stratejisi',
                        'Yerel SEO ve lokasyon bazlı reklam optimizasyonu',
                        'Premium hizmet algısını destekleyen görsel kimlik ve içerik stratejisi'
                    ];
                    
                    insightsList.innerHTML = '';
                    fallbackInsights.forEach(insight => {
                        const li = document.createElement('li');
                        li.className = 'flex items-start space-x-2 mb-2';
                        li.innerHTML = `
                            <span class="w-2 h-2 bg-primary rounded-full mt-2 flex-shrink-0"></span>
                            <span class="text-text">${insight}</span>
                        `;
                        insightsList.appendChild(li);
                    });
                }
                
                // Animate sections in
                setTimeout(() => {
                    sections.forEach((section, index) => {
                        setTimeout(() => {
                            section.style.opacity = '1';
                            section.classList.add('fade-in');
                        }, index * 150);
                    });
                }, 100);
                
                if (window.showToast) {
                    showToast('Örnek rakip analizi yüklendi', 'info', 2000);
                }
            }
        }
        
        // Goal selection handling
        const goalOptions = document.querySelectorAll('.goal-option');
        goalOptions.forEach(option => {
            option.addEventListener('click', function() {
                const goal = this.dataset.goal;
                const radio = this.querySelector('input[type="radio"]');
                
                // Clear all selections first
                goalOptions.forEach(opt => {
                    opt.classList.remove('selected');
                    opt.querySelector('input[type="radio"]').checked = false;
                });
                
                // Select clicked option
                radio.checked = true;
                this.classList.add('selected');
                
                // Add visual feedback
                this.style.transform = 'scale(0.98)';
                setTimeout(() => {
                    this.style.transform = 'scale(1)';
                }, 150);
                
                // Update state
                saveState({ goal: goal });
                
                // Show selection feedback
                showToast(`Hedef seçildi: ${this.querySelector('.font-medium').textContent}`, 'success', 2000);
            });
            
            // Add hover effects
            option.addEventListener('mouseenter', function() {
                if (!this.classList.contains('selected')) {
                    this.style.transform = 'translateY(-2px)';
                }
            });
            
            option.addEventListener('mouseleave', function() {
                if (!this.classList.contains('selected')) {
                    this.style.transform = 'translateY(0)';
                }
            });
        });
        
        function updateGoalSelection(selectedGoal) {
            goalOptions.forEach(option => {
                const goalLabel = option.querySelector('.goal-label');
                if (option.dataset.goal === selectedGoal) {
                    option.classList.add('selected');
                    option.querySelector('input[type="radio"]').checked = true;
                    
                    // Add checkmark icon
                    let checkmark = option.querySelector('.goal-checkmark');
                    if (!checkmark) {
                        checkmark = document.createElement('div');
                        checkmark.className = 'goal-checkmark absolute top-2 right-2 w-6 h-6 bg-primary rounded-full flex items-center justify-center';
                        checkmark.innerHTML = '<svg class="w-3 h-3 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M5 13l4 4L19 7"></path></svg>';
                        goalLabel.style.position = 'relative';
                        goalLabel.appendChild(checkmark);
                    }
                } else {
                    option.classList.remove('selected');
                    option.querySelector('input[type="radio"]').checked = false;
                    
                    // Remove checkmark
                    const checkmark = option.querySelector('.goal-checkmark');
                    if (checkmark) {
                        checkmark.remove();
                    }
                }
            });
        }
    });
</script>

<style>
    .tab-button {
        border-bottom-color: transparent;
        color: #6B7280;
        transition: all 0.3s ease;
    }
    
    .tab-button.active {
        border-bottom-color: #7B61FF;
        color: #7B61FF;
    }
    
    .tab-button:hover {
        color: #7B61FF;
    }
    
    .goal-option {
        cursor: pointer;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        position: relative;
    }
    
    .goal-label {
        display: block;
        padding: 1.25rem;
        border: 2px solid #E5E7EB;
        border-radius: 0.875rem;
        cursor: pointer;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        background: white;
        position: relative;
        overflow: hidden;
    }
    
    .goal-label::before {
        content: '';
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 100%;
        background: linear-gradient(90deg, transparent, rgba(123, 97, 255, 0.1), transparent);
        transition: left 0.5s;
    }
    
    .goal-option:hover .goal-label {
        border-color: #7B61FF;
        background-color: rgba(123, 97, 255, 0.05);
        box-shadow: 0 4px 12px rgba(123, 97, 255, 0.15);
        transform: translateY(-2px);
    }
    
    .goal-option:hover .goal-label::before {
        left: 100%;
    }
    
    .goal-option.selected .goal-label {
        border-color: #7B61FF;
        background: linear-gradient(135deg, rgba(123, 97, 255, 0.1) 0%, rgba(123, 97, 255, 0.05) 100%);
        box-shadow: 0 0 0 3px rgba(123, 97, 255, 0.1), 0 8px 25px rgba(123, 97, 255, 0.2);
        transform: translateY(-1px);
    }
    
    .goal-option.selected .goal-label .font-medium {
        color: #7B61FF;
        font-weight: 600;
    }
    
    .goal-checkmark {
        animation: checkmarkAppear 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55);
    }
    
    @@keyframes checkmarkAppear {
        0% {
            opacity: 0;
            transform: scale(0) rotate(180deg);
        }
        100% {
            opacity: 1;
            transform: scale(1) rotate(0deg);
        }
    }
    
    .goal-option:active {
        transform: scale(0.98);
    }
    
    .analysis-section {
        opacity: 1;
        transform: translateY(0);
        transition: all 0.6s ease;
    }
    
    .analysis-section.fade-in {
        animation: fadeInSection 0.6s ease forwards;
    }
    
    @@keyframes fadeInSection {
        0% {
            opacity: 0;
            transform: translateY(20px);
        }
        100% {
            opacity: 1;
            transform: translateY(0);
        }
    }
    
    /* Loading skeleton */
    .loading-skeleton {
        background: linear-gradient(90deg, #f0f0f0 25%, #e0e0e0 50%, #f0f0f0 75%);
        background-size: 200% 100%;
        animation: shimmer 1.5s infinite;
    }
    
    @@keyframes shimmer {
        0% { background-position: -200% 0; }
        100% { background-position: 200% 0; }
    }
</style>
}